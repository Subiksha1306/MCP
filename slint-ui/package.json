{
  "name": "mcp-desktop-ui",
  "version": "0.1.0",
  "description": "MCP Desktop AI Workspace UI",
  "type": "module",
  "scripts": {
    "build": "slint-viewer --save-build main.slint",
    "dev": "slint-viewer main.slint"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0",
    "slint-ui": "^1.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0"
  }
}